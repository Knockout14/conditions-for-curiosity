# App v1 — status as of Sept 1, 2026

**Read this before touching the app again.** Everything below happened in one
Claude Code session, working from `app-v1-spec-2026-09-01.md`. Nothing here
was decided in advance of that session — it's the record of what got built
and why, so the next session (mine or someone else's) doesn't have to
re-derive it from chat history that won't be there.

## What's live

Pushed to `main`, deployed to production: `conditionsforcuriosity.netlify.app/app/*`.
Noindexed (every page carries `<meta name="robots" content="noindex">`) and
not linked from anywhere on the existing site — reachable by direct URL only.

Built: episode-5 gate → name/email/age capture → three-variable
comprehension check → weekly pick (drag to rank three candidates). Each
check-screen answer is coded correctly regardless of which option gets
tapped, with its own short blurb — not just a single "here's the right
answer" line. Confirming a pick writes a row to the founder's Google Sheet
and shows a ranked summary with a "change your picks" link back in.

Not built: the nightly loop itself (open → the move → reveal → circle-back
→ done — screens 5–8 of the mockup). The "done" gray-out on the summary
screen is already wired to read `askedQuestionIds` from local state, but
nothing populates that list yet — it'll just start working once circle-back
exists.

## The decision that shaped everything else: the bank is proprietary, and this repo is public

Mid-build, the call came down that the question bank — not each question
individually, but the curation and tagging across all 165 — counts as the
thing worth protecting, "at least [as much as] the organization of questions
and information." `conditions-for-curiosity` is a public GitHub repo, so
anything committed to it is exposed regardless of how the app uses it.
Two things followed from that:

**The bank never touches this repo, in any form.** Not the markdown, not a
parsed JSON version, not even gitignored — gitignore only keeps git from
tracking a file, it does nothing to stop `netlify deploy` from uploading
whatever's sitting in the working directory. Proven the hard way: an early
draft deploy briefly served `app/data/questions.json` as a plain static
file despite it being gitignored. Both bad deploys were found and deleted
via `netlify api deleteDeploy` before this got documented anywhere public.
The actual fix was physical, not conventional — the source markdown and its
parsed JSON live outside the repo entirely, in the already-Nextcloud-synced
vault at `Claude Cowork HQ/Context/engineering-curiosity/product/` (that's
where the markdown always lived; the parsed `questions.json` and the parser
script that generates it were added there this session).

**Two Netlify Functions are the only way the app touches the bank**, both
reading from a private Netlify Blobs store (`question-bank`, key `all`)
that the public site can't serve directly:

- `weekly-candidates.mjs` — samples 3 age-matched Raw/Found candidates
  (1 guaranteed Math, 1 guaranteed General-or-Both, 1 free), returns only
  `id`/`text`/`domain`/`bigIdea`/`category`.
- `questions-by-ids.mjs` — full fields (including the adult's answer seed)
  for a specific, already-confirmed set of ids. Hard-capped at 10 ids per
  call so it can't be walked into a bulk dump of the bank.

Updating the bank going forward: edit the markdown in the HQ vault, run
`node parse-bank.js` there, then from this repo —
`netlify blobs:set question-bank all --input "<HQ vault path>/questions.json"`.
Takes effect immediately, no redeploy.

## Submissions: Google Sheets, not Netlify-native

Confirming a weekly pick calls `submit-pick.mjs`, which writes one row
(timestamp, session id, name, email, age band, whether each comprehension
check answer was right, the three ranked questions) to a Sheet via a
service account. Chosen over a second Blobs store specifically because it
comes with a viewer for free — no founder-facing digest page has been built,
and the spreadsheet itself is the digest for now.

Auth is a hand-signed RS256 JWT (`node:crypto`, no `googleapis` dependency)
traded for an OAuth token, straight to the Sheets API. Credentials are three
Netlify env vars — `GOOGLE_SERVICE_ACCOUNT_EMAIL`,
`GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY`, `GOOGLE_SHEET_ID` — set via direct
HTTPS calls to Netlify's API, not the CLI (`netlify env:set` chokes on a
positional value starting with `-----BEGIN PRIVATE KEY-----`, parses it as
a flag). A backup copy of the service-account key JSON lives at
`Claude Cowork HQ/Projects/engineering-curiosity/app/google-sheets-service-account-key.json`
in the synced vault. If it's ever lost: Google Cloud Console → the
`sheets-writer` service account → Keys → delete the old one, create a new
one, re-share the Sheet if needed (same `client_email`, so usually not
even that), reload the env var. Done once already this session; costs about
two minutes.

Quota headroom at expected volume (20–50 uses over the next week or two):
not a real constraint. Netlify Functions free tier is 125,000
invocations/month; Sheets API allows 300 writes/minute/project. Both are
2–3 orders of magnitude above anything this app will do at that scale.

## Open, not forgotten

Self-serve public link vs. founder-invited, for when this opens past the
warm test group — flagged in the original handoff doc, still undecided,
doesn't block anything yet.

## Picking this back up

1. Read this file.
2. `netlify status` / `netlify link` if a new machine — site is
   `conditionsforcuriosity`, account `knockout14`.
3. The vault path above has everything the repo deliberately doesn't:
   the bank source, the parser, the credential backup.
4. Next real chunk of work: the nightly loop (screens 5–8 of the mockup —
   `Main.dc.html`, `Question.dc.html`, `Reveal.dc.html`, `CircleBack.dc.html`
   in the original Claude Design canvas). That's what turns on the
   `askedQuestionIds` tracking the summary screen is already waiting for.
